public abstract class Network {
    String userName;
    String password;

    public Network(){}
    final public boolean post(String message){
        if()
        
        return false;

    }
    abstract boolean login(String userName, String password);
    abstract boolean sendMessage(byte[] data);
}
